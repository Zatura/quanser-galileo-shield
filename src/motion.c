#include <stdio.h>

int move(float angle, int time){
  printf("angle: %f\n", angle);
  printf("time: %d\n\n", time);
  while(1){
    //TODO
  }
  return 0;
}

int move_to_angle(float angle){
  printf("angle: %f\n", angle);
  while(1){
    //TODO
  }
  return 0;
}
